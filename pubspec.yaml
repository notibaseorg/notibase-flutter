name: notibase_flutter
# 60–180 characters. Over the ceiling pub.dev docks ten points and truncates
# it in search results anyway, which is the only place most people will ever
# read it.
description: >-
  Notibase for Flutter: push notifications, in-app messages and an in-app
  inbox. Pure Dart, no platform channels — pass the firebase_messaging token
  and the rest is one call.
version: 0.9.1
homepage: https://notibase.dev/flutter.html
repository: https://github.com/notibaseorg/notibase-flutter
issue_tracker: https://github.com/notibaseorg/notibase-flutter/issues
documentation: https://notibase.dev/flutter.html

# Five is the maximum, and `topic:` is a filter people actually browse with.
# Every one of these is a canonical topic that already exists on pub.dev —
# inventing one puts the package in a list of one, which helps nobody.
topics:
  - push-notifications
  - notifications
  - fcm
  - firebase
  - messaging

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  # The ONLY dependency (Arch §8.2) — device-id persistence.
  #
  # Kept to one deliberately, and the reason is sharper than tidiness:
  # pub.dev derives a package's PLATFORM tags from its dependencies, so a
  # plugin that supports only Android makes this package Android-only on
  # pub.dev — which is what happened in 0.9.0 and is why 0.9.1 exists. Every
  # iOS Flutter developer was told this SDK does not support their platform.
  # See scripts/check-flutter-platforms.mjs before adding a second one.
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  # test-only: HMAC for the e2e's identity-verification checks
  crypto: ^3.0.0
